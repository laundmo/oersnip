test:
  tags:
    - test
    - discord
    - idfk
  required_envs: 
    - clipboard
  template: |
    This is a test template
    that spans multiple lines of text
    and uses the jinja syntax
    heres your clipboard:
    {{CLIPBOARD}}